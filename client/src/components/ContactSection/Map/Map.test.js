import { render } from "@testing-library/react";
import Map from "./Map";

test("should render map component", async () => {});

function sleep(ms) {
  return new Promise((resolve) => setTimeout(resolve, ms));
}
